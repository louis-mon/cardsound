import { Typography } from "@mui/material";
import { Container } from "@mui/system";
import * as React from "react";

export const Config = () => {
  return (
    <Container>
      <Typography variant="h1">Card sound</Typography>
    </Container>
  );
};
